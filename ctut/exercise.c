#include <stdio.h>
int main()
{int a,b; 
        printf("the number a:");
         scanf("%d",&a);
printf("the number b:");
         scanf("%d",&b);
        for ( a<0;b<=10;)
       
        {
    
         printf("the multiplication of a*b:%d",a*b);
         if (b=5)
         {
            break;
            
         }
         
       }


    return 0;
}